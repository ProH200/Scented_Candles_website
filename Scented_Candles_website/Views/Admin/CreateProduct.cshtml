@model ScentedCandleWebsite.Models.Product

@{
    ViewData["Title"] = "Create New Candle";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var categories = ViewBag.Categories as List<ScentedCandleWebsite.Models.Category>;
}

<div class="container-fluid px-4">
    <div class="row justify-content-center">
        <div class="col-xl-10 col-lg-11">
            <div class="card shadow-sm border-0" style="border-radius: 20px; overflow: hidden;">
                <div class="card-header py-3" style="background: #800000; color: white; border: none;">
                    <h4 class="mb-0 fw-bold">
                        <i class="fas fa-plus-circle me-2"></i> Create New Candle
                    </h4>
                </div>
                <div class="card-body" style="background: white; padding: 24px;">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show border-0 rounded-3 shadow-sm" role="alert" style="border-left: 4px solid #dc3545;">
                            <i class="fas fa-exclamation-circle me-2"></i> @TempData["Error"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <form asp-action="CreateProduct" method="post" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger border-0 rounded-3"></div>

                        <div class="row g-3">
                            <div class="col-md-8">
                                <label asp-for="Name" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-candle-holder me-1"></i> Candle Name *
                                </label>
                                <input asp-for="Name" class="form-control" placeholder="e.g., Lavender Dreams" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                <span asp-validation-for="Name" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="Scent" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-leaf me-1"></i> Scent *
                                </label>
                                <select asp-for="Scent" class="form-control" id="scentSelect" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;">
                                    <option value="">Select a scent</option>
                                    <option value="Vanilla">Vanilla</option>
                                    <option value="Lavender">Lavender</option>
                                    <option value="Rose">Rose</option>
                                    <option value="Citrus">Citrus</option>
                                    <option value="Cinnamon">Cinnamon</option>
                                    <option value="Sandalwood">Sandalwood</option>
                                    <option value="Jasmine">Jasmine</option>
                                    <option value="Ocean Breeze">Ocean Breeze</option>
                                    <option value="Eucalyptus">Eucalyptus</option>
                                    <option value="Pumpkin Spice">Pumpkin Spice</option>
                                    <option value="Custom">Custom (Enter Below)</option>
                                </select>
                                <input type="text" id="customScent" class="form-control mt-2" placeholder="Enter custom scent" style="display:none; border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                <span asp-validation-for="Scent" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>
                        </div>

                        <div class="row mt-3">
                            <div class="col-12">
                                <label asp-for="Description" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-align-left me-1"></i> Description *
                                </label>
                                <textarea asp-for="Description" class="form-control" rows="4" placeholder="Describe the candle's fragrance, mood, and benefits..." style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px; resize: vertical;"></textarea>
                                <span asp-validation-for="Description" class="text-danger" style="font-size: 0.85rem;"></span>
                                <small class="form-text text-muted">Include scent notes, mood, and ideal use.</small>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-4">
                                <label asp-for="Price" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-rand me-1"></i> Price *
                                </label>
                                <div class="input-group">
                                    <span class="input-group-text" style="background: #800000; color: white; border: 2px solid #800000; border-radius: 10px 0 0 10px;">$</span>
                                    <input asp-for="Price" class="form-control" placeholder="0.00" id="priceInput" style="border-radius: 0 10px 10px 0; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                </div>
                                <span asp-validation-for="Price" class="text-danger" style="font-size: 0.85rem;"></span>
                                <small class="form-text text-muted">Enter price (e.g., 24.99)</small>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="StockQuantity" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-boxes me-1"></i> Stock Quantity *
                                </label>
                                <input asp-for="StockQuantity" class="form-control" placeholder="0" id="stockInput" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                <span asp-validation-for="StockQuantity" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="CategoryId" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-tag me-1"></i> Category
                                </label>
                                <select asp-for="CategoryId" class="form-control" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;">
                                    <option value="">Select a category</option>
                                    @if (categories != null && categories.Any())
                                    {
                                        foreach (var category in categories)
                                        {
                                            <option value="@category.Id">@category.Name</option>
                                        }
                                    }
                                    else
                                    {
                                        <option value="" disabled>No categories available</option>
                                    }
                                </select>
                                <span asp-validation-for="CategoryId" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-6">
                                <label asp-for="Color" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-palette me-1"></i> Color
                                </label>
                                <input asp-for="Color" class="form-control" placeholder="e.g., Purple, White, Blue" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                <span asp-validation-for="Color" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="BurnTime" class="form-label fw-bold" style="color: #800000;">
                                    <i class="far fa-clock me-1"></i> Burn Time
                                </label>
                                <input asp-for="BurnTime" class="form-control" placeholder="e.g., 30-40 hours" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                <span asp-validation-for="BurnTime" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-6">
                                <label asp-for="WaxType" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-tint me-1"></i> Wax Type
                                </label>
                                <select asp-for="WaxType" class="form-control" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;">
                                    <option value="Soy Wax">Soy Wax</option>
                                    <option value="Beeswax">Beeswax</option>
                                    <option value="Paraffin">Paraffin</option>
                                    <option value="Coconut Wax">Coconut Wax</option>
                                    <option value="Palm Wax">Palm Wax</option>
                                </select>
                                <span asp-validation-for="WaxType" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="Size" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-weight-hanging me-1"></i> Size/Weight
                                </label>
                                <input asp-for="Size" class="form-control" placeholder="e.g., 8oz, 12oz, Large" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                <span asp-validation-for="Size" class="text-danger" style="font-size: 0.85rem;"></span>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-6">
                                <div class="form-check form-switch p-3" style="background: rgba(128, 0, 0, 0.03); border-radius: 10px;">
                                    <input asp-for="IsFeatured" class="form-check-input" type="checkbox" role="switch" style="width: 40px; height: 20px;">
                                    <label asp-for="IsFeatured" class="form-check-label fw-bold" style="color: #800000;">
                                        <i class="fas fa-star me-1"></i> Featured Product
                                    </label>
                                    <br />
                                    <small class="form-text text-muted">Featured products appear on the homepage.</small>
                                </div>
                            </div>

                            <div class="col-md-6">
                                <div class="form-check form-switch p-3" style="background: rgba(128, 0, 0, 0.03); border-radius: 10px;">
                                    <input asp-for="IsOnSale" class="form-check-input" type="checkbox" role="switch" id="onSaleToggle" style="width: 40px; height: 20px;">
                                    <label asp-for="IsOnSale" class="form-check-label fw-bold" style="color: #800000;">
                                        <i class="fas fa-tag me-1"></i> On Sale
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div id="salePriceSection" style="display:none;">
                            <div class="row g-3 mt-2">
                                <div class="col-md-6">
                                    <label asp-for="SalePrice" class="form-label fw-bold" style="color: #800000;">
                                        <i class="fas fa-dollar-sign me-1"></i> Sale Price
                                    </label>
                                    <div class="input-group">
                                        <span class="input-group-text" style="background: #800000; color: white; border: 2px solid #800000; border-radius: 10px 0 0 10px;">$</span>
                                        <input asp-for="SalePrice" class="form-control" placeholder="0.00" style="border-radius: 0 10px 10px 0; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                    </div>
                                    <span asp-validation-for="SalePrice" class="text-danger" style="font-size: 0.85rem;"></span>
                                </div>

                                <div class="col-md-6">
                                    <label asp-for="DiscountPercentage" class="form-label fw-bold" style="color: #800000;">
                                        <i class="fas fa-percent me-1"></i> Discount Percentage
                                    </label>
                                    <div class="input-group">
                                        <input asp-for="DiscountPercentage" class="form-control" placeholder="0" style="border-radius: 10px 0 0 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                        <span class="input-group-text" style="background: #800000; color: white; border: 2px solid #800000; border-radius: 0 10px 10px 0;">%</span>
                                    </div>
                                    <span asp-validation-for="DiscountPercentage" class="text-danger" style="font-size: 0.85rem;"></span>
                                </div>
                            </div>
                        </div>

                        <div class="row mt-3">
                            <div class="col-12">
                                <label class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-image me-1"></i> Product Image
                                </label>
                                <input type="file" name="productImage" class="form-control" accept="image/*" id="imageUpload" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;" />
                                <small class="form-text text-muted">Upload a high-quality image of your candle. (JPG, PNG, GIF, WEBP)</small>
                                <div id="imagePreview" class="mt-2" style="display:none;">
                                    <img id="preview" src="#" alt="Preview" style="max-width: 200px; max-height: 200px; border-radius: 10px; border: 2px solid #e8d9cc; padding: 5px;" />
                                </div>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-6">
                                <div class="form-check form-switch p-3" style="background: rgba(128, 0, 0, 0.03); border-radius: 10px;">
                                    <input asp-for="IsActive" class="form-check-input" type="checkbox" role="switch" checked style="width: 40px; height: 20px;">
                                    <label asp-for="IsActive" class="form-check-label fw-bold" style="color: #800000;">
                                        <i class="fas fa-power-off me-1"></i> Active
                                    </label>
                                    <br />
                                    <small class="form-text text-muted">Inactive products won't be visible to customers.</small>
                                </div>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="RequiredRole" class="form-label fw-bold" style="color: #800000;">
                                    <i class="fas fa-lock me-1"></i> Access Level
                                </label>
                                <select asp-for="RequiredRole" class="form-control" style="border-radius: 10px; border: 2px solid #e8d9cc; padding: 10px 15px;">
                                    <option value="Customer">Everyone (All Customers)</option>
                                    <option value="Admin">Admin Only (Restricted)</option>
                                </select>
                                <small class="form-text text-muted">Restrict this candle to specific user roles.</small>
                            </div>
                        </div>

                        <div class="alert mt-4" style="background: rgba(128, 0, 0, 0.05); border: 1px solid rgba(128, 0, 0, 0.15); border-radius: 12px; color: #4a0000;">
                            <i class="fas fa-info-circle" style="color: #800000;"></i>
                            <strong style="color: #800000;"> Tips for creating great candles:</strong>
                            <ul class="mb-0 mt-2" style="color: #4a0000;">
                                <li>Use descriptive names that evoke the scent</li>
                                <li>Include fragrance notes in the description</li>
                                <li>Set appropriate stock levels based on your inventory</li>
                                <li>Upload clear, well-lit product images</li>
                            </ul>
                        </div>

                        <div class="d-flex flex-wrap gap-2 justify-content-end mt-4 pt-3" style="border-top: 2px solid #f0e4d8;">
                            <a asp-action="Products" class="btn" style="background: white; color: #800000; border: 2px solid #800000; border-radius: 10px; padding: 10px 24px; font-weight: 500;">
                                <i class="fas fa-times me-1"></i> Cancel
                            </a>
                            <button type="submit" class="btn" style="background: #800000; color: white; border: none; border-radius: 10px; padding: 10px 24px; font-weight: 500;">
                                <i class="fas fa-save me-1"></i> Create Candle
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        // Handle custom scent
        const scentSelect = document.getElementById('scentSelect');
        const customScentInput = document.getElementById('customScent');

        if (scentSelect) {
            scentSelect.addEventListener('change', function () {
                if (this.value === 'Custom') {
                    customScentInput.style.display = 'block';
                    customScentInput.required = true;
                    customScentInput.name = 'Scent';
                    this.name = '';
                } else {
                    customScentInput.style.display = 'none';
                    customScentInput.required = false;
                    customScentInput.name = '';
                    this.name = 'Scent';
                }
            });
        }

        // Handle On Sale toggle
        const onSaleToggle = document.getElementById('onSaleToggle');
        const salePriceSection = document.getElementById('salePriceSection');

        if (onSaleToggle) {
            onSaleToggle.addEventListener('change', function () {
                if (this.checked) {
                    salePriceSection.style.display = 'block';
                } else {
                    salePriceSection.style.display = 'none';
                    document.querySelector('[asp-for="SalePrice"]').value = '';
                    document.querySelector('[asp-for="DiscountPercentage"]').value = '';
                }
            });
        }

        // Price formatting
        const priceInput = document.getElementById('priceInput');
        if (priceInput) {
            priceInput.addEventListener('input', function (e) {
                this.value = this.value.replace(/[^0-9.]/g, '');
                let value = parseFloat(this.value);
                if (!isNaN(value) && value > 0) {
                    this.value = value.toFixed(2);
                }
            });
        }

        // Stock quantity validation
        const stockInput = document.getElementById('stockInput');
        if (stockInput) {
            stockInput.addEventListener('input', function (e) {
                let value = parseInt(this.value);
                if (isNaN(value)) this.value = 0;
                if (value < 0) this.value = 0;
            });
        }

        // Image preview
        const imageUpload = document.getElementById('imageUpload');
        const imagePreview = document.getElementById('imagePreview');
        const preview = document.getElementById('preview');

        if (imageUpload) {
            imageUpload.addEventListener('change', function (e) {
                const file = e.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function (e) {
                        preview.src = e.target.result;
                        imagePreview.style.display = 'block';
                    }
                    reader.readAsDataURL(file);
                }
            });
        }
    </script>
}